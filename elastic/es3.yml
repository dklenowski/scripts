cluster.name: es
node.name: 3
node.master: false
node.data: true

# performance stuff
index.number_of_shards: 3     # cannot change
index.number_of_replicas: 2

path.data: /opt/elasticsearch-1.2.1/data/3
path.logs: /opt/elasticsearch-1.2.1/logs/3

network.host: localhost

discovery.zen.ping.multicast.enabled: false
discovery.zen.ping.unicast.hosts: ["localhost"]

